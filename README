# Pacemaker

# vmware workstation 16 install 

# vmware workstation 16 ) vm machine 2개 ( kvm , vbmc) install / 

# vmware workstation 16 ) kvm - centos 7.9 cpu check 가상화 / memory 8GB / cpu core / GUI / DISK 100GB

# vmware workstaiton 16 ) vbmc - centos 7.9 최소설치 / memory 1GB / disk 15GB

# 

# kvm vm ) KVM (libvirt) install

# vbmc ) vbmc install

# vbmc ) setting

# kvm vm ) vm machine create 3 (ha1 ha2 storage)

# ha1 vm ) pacemaker create script

# ha1 vm ) fence_ipmilan or fence_xvm setting

# storage vm ) install Iscsi

# ha1 , ha2 ) volume check , multipath

# ha1 ) ha-lvm configure

# ha1 ) resource 1 : apachewebserver

# ha1 ) resource 2 : apachetomcat

# ha1 ) resource 3 : ping

# ha1 ) resource 4 : LVM

# ha1 ) resource 5 : FileSystem

# ha1 ) resource 6 : oracle



