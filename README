# Pacemaker

# vmware workstation 16 
# vmware workstation 16 ) vm machine 2개 ( kvm , ipmi_node ) install / centos 7.9 cpu check / memory 8GB / cpu core / GUI
# kvm vm ) KVM (libvirt) install 
# kvm vm ) vbmc install
# ipmi_node )
# kvm vm ) vm machine create 3 (ha1 ha2 storage)
# ha1 ) pacemaker create script
# ha1 ) fence_ipmilan or fence_xvm
# storage ) install Iscsi
# ha1 , ha2 ) volume check , multipath
# ha1 ) ha-lvm configure
# ha1 ) resource 1 : apachewebserver
# ha1 ) resource 2 : apachetomcat
# ha1 ) resource 3 : ping
# ha1 ) resource 4 : LVM
# ha1 ) resource 5 : FileSystem
# ha1 ) resource 6 : oracle



